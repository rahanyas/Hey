// import { Server } from "socket.io";
// import { uri } from "./app";

// const io = new Server(uri);

// io.on('connection', (socket, callback) => {
//      console.log('socket : ', socket.id);
//      callback('connnected to backend socket')
// })