# why added vercel.json
did this because no matter what url the user visits - /, /login,/profile etc. the server will serve index.html(tne
root page)

# things to make 

add style to nav btn = done

decrese the size of welcome txt in mobile =done

navbar not changig to islogged in true state when user is loging through google = solved

in prod when logging out and when hit refresh , its going to homepage = solved

when user login through google , not redirecting to hompage , 
checkauth giving false value for isLogedIn but cookie is setting 
=  solved , issue : brave is blocking third party cookies

